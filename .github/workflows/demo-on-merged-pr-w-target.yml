name: Demo on merged PR
on: 
  pull_request:
    types:
      - closed
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: echo "This pull request is targeting ${{ github.event.pull_request_target }} 🎯"
    - run: echo "The PR was merged 🥃"
